<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ backup/transfer policy. Exclude everything: the DB (receipts, allergies) is private, and
     SecureStorage's Keystore-wrapped creds cannot be decrypted after a restore onto a different device. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
