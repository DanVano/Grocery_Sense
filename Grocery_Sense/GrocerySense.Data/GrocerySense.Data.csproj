<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" Version="10.0.9" />
    <!-- Deliberate pin: bumps the native SQLite engine past the 2.1.11 that Microsoft.Data.Sqlite pulls transitively. -->
    <PackageReference Include="SQLitePCLRaw.bundle_e_sqlite3" Version="3.0.3" />
  </ItemGroup>

  <ItemGroup>
    <!-- ItemsAdminRepo.ItemIdTables is internal but the schema-drift guard test asserts against it directly. -->
    <InternalsVisibleTo Include="GrocerySense.Tests" />
  </ItemGroup>

</Project>

